<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <a href="/raca/racaCreate">Adicionar Racas</a>
    <ul>
      <% if (racas && racas.length) { %>  <!-- Ensure gatos exists and has items -->
        <% racas.forEach((raca) => { %>
          <li>
                
            <a href="/raca/<%=raca.id%>/racaUpdate">editar</a>
            <a href = "/raca/<%=raca.raca%>/gatosList"> <%= raca.raca%></h3>
              <form action="raca/<%= raca.raca %>/racaDelete" method="POST" style="display:inline;">
                <button type="submit" onclick="return confirm('Are you sure you want to delete this user?');">Delete</button>
              </form>
          </li>
        <% }) %>
      <% } else { %>
        <li>No raca Found found</li>
      <% } %>
    </ul>
</body>
</html>
